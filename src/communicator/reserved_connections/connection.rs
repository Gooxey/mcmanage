//! This module provides the [`Connection`] struct which will be used by the communicator of the main application to verify if the connected client really is a worker.


/// This struct describes a connection. It will be used by the communicator of the main application to verify if the connected client really is a worker.
pub struct Connection;